# 六爻起卦系统

一个基于传统六爻占卜的起卦系统，支持多种起卦方式。

## 功能特点

- **标的物起卦**：根据输入的标的物名称（如股票、婚姻、事业等），结合笔画数和当前时间起卦
- **硬币起卦**：模拟传统铜钱起卦，支持系统自动抛硬币和手动输入硬币结果两种方式
- **数字起卦**：输入两个数字分别作为上下卦进行起卦
- **六神排盘**：根据日天干自动排六神
- **世应定位**：自动计算世爻和应爻位置
- **地支配置**：根据卦象自动配置六爻地支
- **六亲配置**：根据卦宫自动配置六亲

## 环境要求

- Python 3.8 或更高版本
- 支持系统：Windows / macOS / Linux

## 快速开始

### Windows 用户（推荐）

双击运行 `setup.bat` 即可自动配置环境并安装依赖。

脚本会：
1. 检查 Python 版本（需要 >= 3.8）
2. 询问是否创建虚拟环境（推荐）
3. 自动安装所有依赖
4. 初始化汉字笔画数据
5. 验证安装是否成功

### 手动安装

```bash
# 1. 创建虚拟环境（可选但推荐）
python -m venv venv

# 2. 激活虚拟环境
# Windows:
venv\Scripts\activate.bat
# macOS/Linux:
source venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 运行程序
python main.py
```

### 国内用户加速

如果下载依赖速度较慢，可以使用国内镜像源：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

或配置永久使用：

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

## 项目结构

```
6yao/
├── main.py              # 主程序入口
├── divination.py        # 起卦核心算法
├── stroke_counter.py    # 繁体字笔画计算
├── requirements.txt     # 依赖列表
├── setup.bat           # Windows 一键配置脚本
└── README.md           # 说明文档
```

## 使用说明

运行 `python main.py` 后，根据菜单提示选择起卦方式：

```
==================================================
       六爻起卦系统
==================================================
1. 标的物起卦（笔画+日时）
2. 硬币起卦
3. 数字起卦（上下卦分开输入）
0. 退出
==================================================
```

### 硬币起卦说明

硬币起卦遵循传统六爻金钱卦规则：

| 阳面数量 | 爻的性质 | 阴阳属性 | 是否动爻 |
|---------|---------|---------|---------|
| 3个阳面 | 老阳 | 阳爻 | 是（动→变阴） |
| 2个阳面 | 少阳 | 阳爻 | 否（静） |
| 1个阳面 | 少阴 | 阴爻 | 否（静） |
| 0个阳面 | 老阴 | 阴爻 | 是（动→变阳） |

手动输入格式：`1 1 0` 表示两个正面一个反面（1=正面/阳，0=反面/阴）

### 子时跨日说明

在传统命理学中，**子时（23:00-01:00）属于下一天**。系统已实现此规则：

- **晚子时（23:00-23:59）**：使用下一天的农历日期
- **早子时（00:00-00:59）**：使用当天的农历日期

例如：2026年3月27日 23:20 被算作农历二月十日（而非二月九日）

## 依赖说明

- **LunarCalendar**: 农历日期计算
- **unihan-etl**: 汉字笔画数据
- **opencc-python-reimplemented**: 简繁转换